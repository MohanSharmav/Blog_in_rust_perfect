[package]
name = "blog-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blog-storage = { path = "../blog-storage" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
proptest = "1"
